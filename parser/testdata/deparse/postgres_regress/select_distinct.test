== 001
|--
-- SELECT_DISTINCT
|--

|--
-- awk '{print $3;}' onek.data | sort -n | uniq
|--
SELECT DISTINCT two FROM onek ORDER BY 1
--
SELECT DISTINCT two FROM onek ORDER BY 1
== 002
|--
-- awk '{print $5;}' onek.data | sort -n | uniq
|--
SELECT DISTINCT ten FROM onek ORDER BY 1
--
SELECT DISTINCT ten FROM onek ORDER BY 1
== 003
|--
-- awk '{print $16;}' onek.data | sort -d | uniq
|--
SELECT DISTINCT string4 FROM onek ORDER BY 1
--
SELECT DISTINCT string4 FROM onek ORDER BY 1
== 004
|--
-- awk '{print $3,$16,$5;}' onek.data | sort -d | uniq |
-- sort +0n -1 +1d -2 +2n -3
|--
SELECT DISTINCT two, string4, ten
   FROM onek
   ORDER BY two using <, string4 using <, ten using <
--
SELECT DISTINCT two, string4, ten FROM onek ORDER BY two USING <, string4 USING <, ten USING <
== 005
|--
-- awk '{print $2;}' person.data |
-- awk '{if(NF!=1){print $2;}else{print;}}' - emp.data |
-- awk '{if(NF!=1){print $2;}else{print;}}' - student.data |
-- awk 'BEGIN{FS="      ";}{if(NF!=1){print $5;}else{print;}}' - stud_emp.data |
-- sort -n -r | uniq
|--
SELECT DISTINCT p.age FROM person* p ORDER BY age using >
--
SELECT DISTINCT p.age FROM person p ORDER BY age USING >
== 006
|--
-- Check mentioning same column more than once
|--

EXPLAIN (VERBOSE, COSTS OFF)
SELECT count(*) FROM
  (SELECT DISTINCT two, four, two FROM tenk1) ss
--
EXPLAIN (VERBOSE, COSTS OFF) SELECT count(*) FROM (SELECT DISTINCT two, four, two FROM tenk1) ss
== 007
SELECT count(*) FROM
  (SELECT DISTINCT two, four, two FROM tenk1) ss
--
SELECT count(*) FROM (SELECT DISTINCT two, four, two FROM tenk1) ss
== 008
|--
-- Compare results between plans using sorting and plans using hash
-- aggregation. Force spilling in both cases by setting work_mem low.
|--

SET work_mem='64kB'
--
SET work_mem TO "64kB"
== 009
-- Produce results with sorting.

SET enable_hashagg=FALSE
--
SET enable_hashagg TO FALSE
== 010
SET jit_above_cost=0
--
SET jit_above_cost TO 0
== 011
EXPLAIN (costs off)
SELECT DISTINCT g%1000 FROM generate_series(0,9999) g
--
EXPLAIN (COSTS OFF) SELECT DISTINCT g % 1000 FROM generate_series(0, 9999) g
== 012
CREATE TABLE distinct_group_1 AS
SELECT DISTINCT g%1000 FROM generate_series(0,9999) g
--
CREATE TABLE distinct_group_1 AS SELECT DISTINCT g % 1000 FROM generate_series(0, 9999) g
== 013
SET jit_above_cost TO DEFAULT
--
SET jit_above_cost TO DEFAULT
== 014
CREATE TABLE distinct_group_2 AS
SELECT DISTINCT (g%1000)::text FROM generate_series(0,9999) g
--
CREATE TABLE distinct_group_2 AS SELECT DISTINCT (g % 1000)::text FROM generate_series(0, 9999) g
== 015
SET enable_seqscan = 0
--
SET enable_seqscan TO 0
== 016
-- Check to see we get an incremental sort plan
EXPLAIN (costs off)
SELECT DISTINCT hundred, two FROM tenk1
--
EXPLAIN (COSTS OFF) SELECT DISTINCT hundred, two FROM tenk1
== 017
RESET enable_seqscan
--
RESET enable_seqscan
== 018
SET enable_hashagg=TRUE
--
SET enable_hashagg TO TRUE
== 019
-- Produce results with hash aggregation.

SET enable_sort=FALSE
--
SET enable_sort TO FALSE
== 020
SET jit_above_cost=0
--
SET jit_above_cost TO 0
== 021
EXPLAIN (costs off)
SELECT DISTINCT g%1000 FROM generate_series(0,9999) g
--
EXPLAIN (COSTS OFF) SELECT DISTINCT g % 1000 FROM generate_series(0, 9999) g
== 022
CREATE TABLE distinct_hash_1 AS
SELECT DISTINCT g%1000 FROM generate_series(0,9999) g
--
CREATE TABLE distinct_hash_1 AS SELECT DISTINCT g % 1000 FROM generate_series(0, 9999) g
== 023
SET jit_above_cost TO DEFAULT
--
SET jit_above_cost TO DEFAULT
== 024
CREATE TABLE distinct_hash_2 AS
SELECT DISTINCT (g%1000)::text FROM generate_series(0,9999) g
--
CREATE TABLE distinct_hash_2 AS SELECT DISTINCT (g % 1000)::text FROM generate_series(0, 9999) g
== 025
SET enable_sort=TRUE
--
SET enable_sort TO TRUE
== 026
SET work_mem TO DEFAULT
--
SET work_mem TO DEFAULT
== 027
-- Compare results

(SELECT * FROM distinct_hash_1 EXCEPT SELECT * FROM distinct_group_1)
  UNION ALL
(SELECT * FROM distinct_group_1 EXCEPT SELECT * FROM distinct_hash_1)
--
(SELECT * FROM distinct_hash_1 EXCEPT SELECT * FROM distinct_group_1) UNION ALL (SELECT * FROM distinct_group_1 EXCEPT SELECT * FROM distinct_hash_1)
== 028
(SELECT * FROM distinct_hash_1 EXCEPT SELECT * FROM distinct_group_1)
  UNION ALL
(SELECT * FROM distinct_group_1 EXCEPT SELECT * FROM distinct_hash_1)
--
(SELECT * FROM distinct_hash_1 EXCEPT SELECT * FROM distinct_group_1) UNION ALL (SELECT * FROM distinct_group_1 EXCEPT SELECT * FROM distinct_hash_1)
== 029
DROP TABLE distinct_hash_1
--
DROP TABLE distinct_hash_1
== 030
DROP TABLE distinct_hash_2
--
DROP TABLE distinct_hash_2
== 031
DROP TABLE distinct_group_1
--
DROP TABLE distinct_group_1
== 032
DROP TABLE distinct_group_2
--
DROP TABLE distinct_group_2
== 033
-- Test parallel DISTINCT
SET parallel_tuple_cost=0
--
SET parallel_tuple_cost TO 0
== 034
SET parallel_setup_cost=0
--
SET parallel_setup_cost TO 0
== 035
SET min_parallel_table_scan_size=0
--
SET min_parallel_table_scan_size TO 0
== 036
SET max_parallel_workers_per_gather=2
--
SET max_parallel_workers_per_gather TO 2
== 037
-- Ensure we get a parallel plan
EXPLAIN (costs off)
SELECT DISTINCT four FROM tenk1
--
EXPLAIN (COSTS OFF) SELECT DISTINCT four FROM tenk1
== 038
-- Ensure the parallel plan produces the correct results
SELECT DISTINCT four FROM tenk1
--
SELECT DISTINCT four FROM tenk1
== 039
CREATE OR REPLACE FUNCTION distinct_func(a INT) RETURNS INT AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL UNSAFE
--
CREATE OR REPLACE FUNCTION distinct_func(a int) RETURNS int AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL unsafe
== 040
-- Ensure we don't do parallel distinct with a parallel unsafe function
EXPLAIN (COSTS OFF)
SELECT DISTINCT distinct_func(1) FROM tenk1
--
EXPLAIN (COSTS OFF) SELECT DISTINCT distinct_func(1) FROM tenk1
== 041
-- make the function parallel safe
CREATE OR REPLACE FUNCTION distinct_func(a INT) RETURNS INT AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL SAFE
--
CREATE OR REPLACE FUNCTION distinct_func(a int) RETURNS int AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL safe
== 042
-- Ensure we do parallel distinct now that the function is parallel safe
EXPLAIN (COSTS OFF)
SELECT DISTINCT distinct_func(1) FROM tenk1
--
EXPLAIN (COSTS OFF) SELECT DISTINCT distinct_func(1) FROM tenk1
== 043
RESET max_parallel_workers_per_gather
--
RESET max_parallel_workers_per_gather
== 044
RESET min_parallel_table_scan_size
--
RESET min_parallel_table_scan_size
== 045
RESET parallel_setup_cost
--
RESET parallel_setup_cost
== 046
RESET parallel_tuple_cost
--
RESET parallel_tuple_cost
== 047
|--
-- Test the planner's ability to use a LIMIT 1 instead of a Unique node when
-- all of the distinct_pathkeys have been marked as redundant
|--

-- Ensure we get a plan with a Limit 1
EXPLAIN (COSTS OFF)
SELECT DISTINCT four FROM tenk1 WHERE four = 0
--
EXPLAIN (COSTS OFF) SELECT DISTINCT four FROM tenk1 WHERE four = 0
== 048
-- Ensure the above gives us the correct result
SELECT DISTINCT four FROM tenk1 WHERE four = 0
--
SELECT DISTINCT four FROM tenk1 WHERE four = 0
== 049
-- Ensure we get a plan with a Limit 1
EXPLAIN (COSTS OFF)
SELECT DISTINCT four FROM tenk1 WHERE four = 0 AND two <> 0
--
EXPLAIN (COSTS OFF) SELECT DISTINCT four FROM tenk1 WHERE four = 0 AND two <> 0
== 050
-- Ensure no rows are returned
SELECT DISTINCT four FROM tenk1 WHERE four = 0 AND two <> 0
--
SELECT DISTINCT four FROM tenk1 WHERE four = 0 AND two <> 0
== 051
-- Ensure we get a plan with a Limit 1 when the SELECT list contains constants
EXPLAIN (COSTS OFF)
SELECT DISTINCT four,1,2,3 FROM tenk1 WHERE four = 0
--
EXPLAIN (COSTS OFF) SELECT DISTINCT four, 1, 2, 3 FROM tenk1 WHERE four = 0
== 052
-- Ensure we only get 1 row
SELECT DISTINCT four,1,2,3 FROM tenk1 WHERE four = 0
--
SELECT DISTINCT four, 1, 2, 3 FROM tenk1 WHERE four = 0
== 053
SET parallel_setup_cost=0
--
SET parallel_setup_cost TO 0
== 054
SET min_parallel_table_scan_size=0
--
SET min_parallel_table_scan_size TO 0
== 055
SET max_parallel_workers_per_gather=2
--
SET max_parallel_workers_per_gather TO 2
== 056
-- Ensure we get a plan with a Limit 1 in both partial distinct and final
-- distinct
EXPLAIN (COSTS OFF)
SELECT DISTINCT four FROM tenk1 WHERE four = 10
--
EXPLAIN (COSTS OFF) SELECT DISTINCT four FROM tenk1 WHERE four = 10
== 057
RESET max_parallel_workers_per_gather
--
RESET max_parallel_workers_per_gather
== 058
RESET min_parallel_table_scan_size
--
RESET min_parallel_table_scan_size
== 059
RESET parallel_setup_cost
--
RESET parallel_setup_cost
== 060
|--
-- Also, some tests of IS DISTINCT FROM, which doesn't quite deserve its
-- very own regression file.
|--

CREATE TEMP TABLE disttable (f1 integer)
--
CREATE TEMPORARY TABLE disttable (f1 int)
== 061
INSERT INTO DISTTABLE VALUES(1)
--
INSERT INTO disttable VALUES (1)
== 062
INSERT INTO DISTTABLE VALUES(2)
--
INSERT INTO disttable VALUES (2)
== 063
INSERT INTO DISTTABLE VALUES(3)
--
INSERT INTO disttable VALUES (3)
== 064
INSERT INTO DISTTABLE VALUES(NULL)
--
INSERT INTO disttable VALUES (NULL)
== 065
-- basic cases
SELECT f1, f1 IS DISTINCT FROM 2 as "not 2" FROM disttable
--
SELECT f1, f1 IS DISTINCT FROM 2 AS "not 2" FROM disttable
== 066
SELECT f1, f1 IS DISTINCT FROM NULL as "not null" FROM disttable
--
SELECT f1, f1 IS DISTINCT FROM NULL AS "not null" FROM disttable
== 067
SELECT f1, f1 IS DISTINCT FROM f1 as "false" FROM disttable
--
SELECT f1, f1 IS DISTINCT FROM f1 AS "false" FROM disttable
== 068
SELECT f1, f1 IS DISTINCT FROM f1+1 as "not null" FROM disttable
--
SELECT f1, f1 IS DISTINCT FROM (f1 + 1) AS "not null" FROM disttable
== 069
-- check that optimizer constant-folds it properly
SELECT 1 IS DISTINCT FROM 2 as "yes"
--
SELECT 1 IS DISTINCT FROM 2 AS yes
== 070
SELECT 2 IS DISTINCT FROM 2 as "no"
--
SELECT 2 IS DISTINCT FROM 2 AS no
== 071
SELECT 2 IS DISTINCT FROM null as "yes"
--
SELECT 2 IS DISTINCT FROM NULL AS yes
== 072
SELECT null IS DISTINCT FROM null as "no"
--
SELECT NULL IS DISTINCT FROM NULL AS no
== 073
-- negated form
SELECT 1 IS NOT DISTINCT FROM 2 as "no"
--
SELECT 1 IS NOT DISTINCT FROM 2 AS no
== 074
SELECT 2 IS NOT DISTINCT FROM 2 as "yes"
--
SELECT 2 IS NOT DISTINCT FROM 2 AS yes
== 075
SELECT 2 IS NOT DISTINCT FROM null as "no"
--
SELECT 2 IS NOT DISTINCT FROM NULL AS no
== 076
SELECT null IS NOT DISTINCT FROM null as "yes"
--
SELECT NULL IS NOT DISTINCT FROM NULL AS yes
== 077
|--
-- Test the planner's ability to reorder the distinctClause Pathkeys to match
-- the input path's ordering
|--

CREATE TABLE distinct_tbl (x int, y int)
--
CREATE TABLE distinct_tbl (x int, y int)
== 078
INSERT INTO distinct_tbl SELECT i%10, i%10 FROM generate_series(1, 1000) AS i
--
INSERT INTO distinct_tbl SELECT i % 10, i % 10 FROM generate_series(1, 1000) i
== 079
CREATE INDEX distinct_tbl_x_y_idx ON distinct_tbl (x, y)
--
CREATE INDEX distinct_tbl_x_y_idx ON distinct_tbl USING btree (x, y)
== 080
ANALYZE distinct_tbl
--
ANALYZE distinct_tbl
== 081
-- Produce results with sorting.
SET enable_hashagg TO OFF
--
SET enable_hashagg TO OFF
== 082
-- Ensure we avoid the need to re-sort by reordering the distinctClause
-- Pathkeys to match the ordering of the input path
EXPLAIN (COSTS OFF)
SELECT DISTINCT y, x FROM distinct_tbl
--
EXPLAIN (COSTS OFF) SELECT DISTINCT y, x FROM distinct_tbl
== 083
SELECT DISTINCT y, x FROM distinct_tbl
--
SELECT DISTINCT y, x FROM distinct_tbl
== 084
-- Ensure we leverage incremental-sort by reordering the distinctClause
-- Pathkeys to partially match the ordering of the input path
EXPLAIN (COSTS OFF)
SELECT DISTINCT y, x FROM (SELECT * FROM distinct_tbl ORDER BY x) s
--
EXPLAIN (COSTS OFF) SELECT DISTINCT y, x FROM (SELECT * FROM distinct_tbl ORDER BY x) s
== 085
SELECT DISTINCT y, x FROM (SELECT * FROM distinct_tbl ORDER BY x) s
--
SELECT DISTINCT y, x FROM (SELECT * FROM distinct_tbl ORDER BY x) s
== 086
-- Ensure we avoid the need to re-sort in partial distinct by reordering the
-- distinctClause Pathkeys to match the ordering of the input path
SET parallel_tuple_cost=0
--
SET parallel_tuple_cost TO 0
== 087
SET parallel_setup_cost=0
--
SET parallel_setup_cost TO 0
== 088
SET min_parallel_table_scan_size=0
--
SET min_parallel_table_scan_size TO 0
== 089
SET min_parallel_index_scan_size=0
--
SET min_parallel_index_scan_size TO 0
== 090
SET max_parallel_workers_per_gather=2
--
SET max_parallel_workers_per_gather TO 2
== 091
EXPLAIN (COSTS OFF)
SELECT DISTINCT y, x FROM distinct_tbl limit 10
--
EXPLAIN (COSTS OFF) SELECT DISTINCT y, x FROM distinct_tbl LIMIT 10
== 092
SELECT DISTINCT y, x FROM distinct_tbl limit 10
--
SELECT DISTINCT y, x FROM distinct_tbl LIMIT 10
== 093
RESET max_parallel_workers_per_gather
--
RESET max_parallel_workers_per_gather
== 094
RESET min_parallel_index_scan_size
--
RESET min_parallel_index_scan_size
== 095
RESET min_parallel_table_scan_size
--
RESET min_parallel_table_scan_size
== 096
RESET parallel_setup_cost
--
RESET parallel_setup_cost
== 097
RESET parallel_tuple_cost
--
RESET parallel_tuple_cost
== 098
-- Ensure we reorder the distinctClause Pathkeys to match the ordering of the
-- input path even if there is ORDER BY clause
EXPLAIN (COSTS OFF)
SELECT DISTINCT y, x FROM distinct_tbl ORDER BY y
--
EXPLAIN (COSTS OFF) SELECT DISTINCT y, x FROM distinct_tbl ORDER BY y
== 099
SELECT DISTINCT y, x FROM distinct_tbl ORDER BY y
--
SELECT DISTINCT y, x FROM distinct_tbl ORDER BY y
== 100
RESET enable_hashagg
--
RESET enable_hashagg
== 101
DROP TABLE distinct_tbl
--
DROP TABLE distinct_tbl
