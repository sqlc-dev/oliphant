== 001
SELECT NOT(pg_numa_available()) AS skip_test
--
SELECT NOT pg_numa_available() AS skip_test
== 002
SELECT COUNT(*) = 0 AS ok FROM pg_shmem_allocations_numa
--
SELECT count(*) = 0 AS ok FROM pg_shmem_allocations_numa
== 003
-- switch to superuser

SELECT COUNT(*) >= 0 AS ok FROM pg_shmem_allocations_numa
--
SELECT count(*) >= 0 AS ok FROM pg_shmem_allocations_numa
