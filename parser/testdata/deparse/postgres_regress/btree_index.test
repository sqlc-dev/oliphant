== 001
|--
-- BTREE_INDEX
|--

-- directory paths are passed to us in environment variables

CREATE TABLE bt_i4_heap (
	seqno 		int4,
	random 		int4
)
--
CREATE TABLE bt_i4_heap (seqno int4, random int4)
== 002
CREATE TABLE bt_name_heap (
	seqno 		name,
	random 		int4
)
--
CREATE TABLE bt_name_heap (seqno name, random int4)
== 003
CREATE TABLE bt_txt_heap (
	seqno 		text,
	random 		int4
)
--
CREATE TABLE bt_txt_heap (seqno text, random int4)
== 004
CREATE TABLE bt_f8_heap (
	seqno 		float8,
	random 		int4
)
--
CREATE TABLE bt_f8_heap (seqno float8, random int4)
== 005
COPY bt_i4_heap FROM :'filename'
--
ERROR: syntax error at or near ":"
CURSORPOS: 22
FILENAME: scan.l
FUNCNAME: scanner_yyerror
== 006
COPY bt_name_heap FROM :'filename'
--
ERROR: syntax error at or near ":"
CURSORPOS: 24
FILENAME: scan.l
FUNCNAME: scanner_yyerror
== 007
COPY bt_txt_heap FROM :'filename'
--
ERROR: syntax error at or near ":"
CURSORPOS: 23
FILENAME: scan.l
FUNCNAME: scanner_yyerror
== 008
COPY bt_f8_heap FROM :'filename'
--
ERROR: syntax error at or near ":"
CURSORPOS: 22
FILENAME: scan.l
FUNCNAME: scanner_yyerror
== 009
ANALYZE bt_i4_heap
--
ANALYZE bt_i4_heap
== 010
ANALYZE bt_name_heap
--
ANALYZE bt_name_heap
== 011
ANALYZE bt_txt_heap
--
ANALYZE bt_txt_heap
== 012
ANALYZE bt_f8_heap
--
ANALYZE bt_f8_heap
== 013
|--
-- BTREE ascending/descending cases
|--
-- we load int4/text from pure descending data (each key is a new
-- low key) and name/f8 from pure ascending data (each key is a new
-- high key).  we had a bug where new low keys would sometimes be
-- "lost".
|--
CREATE INDEX bt_i4_index ON bt_i4_heap USING btree (seqno int4_ops)
--
CREATE INDEX bt_i4_index ON bt_i4_heap USING btree (seqno int4_ops)
== 014
CREATE INDEX bt_name_index ON bt_name_heap USING btree (seqno name_ops)
--
CREATE INDEX bt_name_index ON bt_name_heap USING btree (seqno name_ops)
== 015
CREATE INDEX bt_txt_index ON bt_txt_heap USING btree (seqno text_ops)
--
CREATE INDEX bt_txt_index ON bt_txt_heap USING btree (seqno text_ops)
== 016
CREATE INDEX bt_f8_index ON bt_f8_heap USING btree (seqno float8_ops)
--
CREATE INDEX bt_f8_index ON bt_f8_heap USING btree (seqno float8_ops)
== 017
|--
-- test retrieval of min/max keys for each index
|--

SELECT b.*
   FROM bt_i4_heap b
   WHERE b.seqno < 1
--
SELECT b.* FROM bt_i4_heap b WHERE b.seqno < 1
== 018
SELECT b.*
   FROM bt_i4_heap b
   WHERE b.seqno >= 9999
--
SELECT b.* FROM bt_i4_heap b WHERE b.seqno >= 9999
== 019
SELECT b.*
   FROM bt_i4_heap b
   WHERE b.seqno = 4500
--
SELECT b.* FROM bt_i4_heap b WHERE b.seqno = 4500
== 020
SELECT b.*
   FROM bt_name_heap b
   WHERE b.seqno < '1'::name
--
SELECT b.* FROM bt_name_heap b WHERE b.seqno < '1'::name
== 021
SELECT b.*
   FROM bt_name_heap b
   WHERE b.seqno >= '9999'::name
--
SELECT b.* FROM bt_name_heap b WHERE b.seqno >= '9999'::name
== 022
SELECT b.*
   FROM bt_name_heap b
   WHERE b.seqno = '4500'::name
--
SELECT b.* FROM bt_name_heap b WHERE b.seqno = '4500'::name
== 023
SELECT b.*
   FROM bt_txt_heap b
   WHERE b.seqno < '1'::text
--
SELECT b.* FROM bt_txt_heap b WHERE b.seqno < '1'::text
== 024
SELECT b.*
   FROM bt_txt_heap b
   WHERE b.seqno >= '9999'::text
--
SELECT b.* FROM bt_txt_heap b WHERE b.seqno >= '9999'::text
== 025
SELECT b.*
   FROM bt_txt_heap b
   WHERE b.seqno = '4500'::text
--
SELECT b.* FROM bt_txt_heap b WHERE b.seqno = '4500'::text
== 026
SELECT b.*
   FROM bt_f8_heap b
   WHERE b.seqno < '1'::float8
--
SELECT b.* FROM bt_f8_heap b WHERE b.seqno < '1'::float8
== 027
SELECT b.*
   FROM bt_f8_heap b
   WHERE b.seqno >= '9999'::float8
--
SELECT b.* FROM bt_f8_heap b WHERE b.seqno >= '9999'::float8
== 028
SELECT b.*
   FROM bt_f8_heap b
   WHERE b.seqno = '4500'::float8
--
SELECT b.* FROM bt_f8_heap b WHERE b.seqno = '4500'::float8
== 029
|--
-- Add coverage of RowCompare quals whose row omits a column ("proargtypes")
-- that's after the first column, but before the final column.  The scan's
-- initial positioning strategy must become >= here (it's not the > strategy,
-- since the absence of "proargtypes" makes that tighter constraint unsafe).
|--
explain (costs off)
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE (proname, pronamespace) > ('abs', 0)
ORDER BY proname, proargtypes, pronamespace LIMIT 1
--
EXPLAIN (COSTS OFF) SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE (proname, pronamespace) > ('abs', 0) ORDER BY proname, proargtypes, pronamespace LIMIT 1
== 030
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE (proname, pronamespace) > ('abs', 0)
ORDER BY proname, proargtypes, pronamespace LIMIT 1
--
SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE (proname, pronamespace) > ('abs', 0) ORDER BY proname, proargtypes, pronamespace LIMIT 1
== 031
|--
-- Similar to the previous test case, but this time it's a backwards scan
-- using a < RowCompare.  Must use the <= strategy (and not the < strategy).
|--
explain (costs off)
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE (proname, pronamespace) < ('abs', 1_000_000)
ORDER BY proname DESC, proargtypes DESC, pronamespace DESC LIMIT 1
--
EXPLAIN (COSTS OFF) SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE (proname, pronamespace) < ('abs', 1000000) ORDER BY proname DESC, proargtypes DESC, pronamespace DESC LIMIT 1
== 032
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE (proname, pronamespace) < ('abs', 1_000_000)
ORDER BY proname DESC, proargtypes DESC, pronamespace DESC LIMIT 1
--
SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE (proname, pronamespace) < ('abs', 1000000) ORDER BY proname DESC, proargtypes DESC, pronamespace DESC LIMIT 1
== 033
|--
-- Forwards scan RowCompare qual whose row arg has a NULL that affects our
-- initial positioning strategy
|--
explain (costs off)
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE (proname, proargtypes) >= ('abs', NULL) AND proname <= 'abs'
ORDER BY proname, proargtypes, pronamespace
--
EXPLAIN (COSTS OFF) SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE (proname, proargtypes) >= ('abs', NULL) AND proname <= 'abs' ORDER BY proname, proargtypes, pronamespace
== 034
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE (proname, proargtypes) >= ('abs', NULL) AND proname <= 'abs'
ORDER BY proname, proargtypes, pronamespace
--
SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE (proname, proargtypes) >= ('abs', NULL) AND proname <= 'abs' ORDER BY proname, proargtypes, pronamespace
== 035
|--
-- Forwards scan RowCompare quals whose row arg has a NULL that ends scan
|--
explain (costs off)
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE proname >= 'abs' AND (proname, proargtypes) < ('abs', NULL)
ORDER BY proname, proargtypes, pronamespace
--
EXPLAIN (COSTS OFF) SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE proname >= 'abs' AND (proname, proargtypes) < ('abs', NULL) ORDER BY proname, proargtypes, pronamespace
== 036
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE proname >= 'abs' AND (proname, proargtypes) < ('abs', NULL)
ORDER BY proname, proargtypes, pronamespace
--
SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE proname >= 'abs' AND (proname, proargtypes) < ('abs', NULL) ORDER BY proname, proargtypes, pronamespace
== 037
|--
-- Backwards scan RowCompare qual whose row arg has a NULL that affects our
-- initial positioning strategy
|--
explain (costs off)
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE proname >= 'abs' AND (proname, proargtypes) <= ('abs', NULL)
ORDER BY proname DESC, proargtypes DESC, pronamespace DESC
--
EXPLAIN (COSTS OFF) SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE proname >= 'abs' AND (proname, proargtypes) <= ('abs', NULL) ORDER BY proname DESC, proargtypes DESC, pronamespace DESC
== 038
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE proname >= 'abs' AND (proname, proargtypes) <= ('abs', NULL)
ORDER BY proname DESC, proargtypes DESC, pronamespace DESC
--
SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE proname >= 'abs' AND (proname, proargtypes) <= ('abs', NULL) ORDER BY proname DESC, proargtypes DESC, pronamespace DESC
== 039
|--
-- Backwards scan RowCompare qual whose row arg has a NULL that ends scan
|--
explain (costs off)
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE (proname, proargtypes) > ('abs', NULL) AND proname <= 'abs'
ORDER BY proname DESC, proargtypes DESC, pronamespace DESC
--
EXPLAIN (COSTS OFF) SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE (proname, proargtypes) > ('abs', NULL) AND proname <= 'abs' ORDER BY proname DESC, proargtypes DESC, pronamespace DESC
== 040
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE (proname, proargtypes) > ('abs', NULL) AND proname <= 'abs'
ORDER BY proname DESC, proargtypes DESC, pronamespace DESC
--
SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE (proname, proargtypes) > ('abs', NULL) AND proname <= 'abs' ORDER BY proname DESC, proargtypes DESC, pronamespace DESC
== 041
-- Makes B-Tree preprocessing deal with unmarking redundant keys that were
-- initially marked required (test case relies on current row compare
-- preprocessing limitations)
explain (costs off)
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE proname = 'zzzzzz' AND (proname, proargtypes) > ('abs', NULL)
   AND pronamespace IN (1, 2, 3) AND proargtypes IN ('26 23', '5077')
ORDER BY proname, proargtypes, pronamespace
--
EXPLAIN (COSTS OFF) SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE proname = 'zzzzzz' AND (proname, proargtypes) > ('abs', NULL) AND pronamespace IN (1, 2, 3) AND proargtypes IN ('26 23', '5077') ORDER BY proname, proargtypes, pronamespace
== 042
SELECT proname, proargtypes, pronamespace
   FROM pg_proc
   WHERE proname = 'zzzzzz' AND (proname, proargtypes) > ('abs', NULL)
   AND pronamespace IN (1, 2, 3) AND proargtypes IN ('26 23', '5077')
ORDER BY proname, proargtypes, pronamespace
--
SELECT proname, proargtypes, pronamespace FROM pg_proc WHERE proname = 'zzzzzz' AND (proname, proargtypes) > ('abs', NULL) AND pronamespace IN (1, 2, 3) AND proargtypes IN ('26 23', '5077') ORDER BY proname, proargtypes, pronamespace
== 043
|--
-- Performs a recheck of > key following array advancement on previous (left
-- sibling) page that used a high key whose attribute value corresponding to
-- the > key was -inf (due to being truncated when the high key was created).
|--
-- XXX This relies on the assumption that tenk1_thous_tenthous has a truncated
-- high key "(183, -inf)" on the first page that we'll scan.  The test will only
-- provide useful coverage when the default 8K BLCKSZ is in use.
|--
explain (costs off)
SELECT thousand, tenthous
  FROM tenk1
  WHERE thousand IN (182, 183) AND tenthous > 7550
--
EXPLAIN (COSTS OFF) SELECT thousand, tenthous FROM tenk1 WHERE thousand IN (182, 183) AND tenthous > 7550
== 044
SELECT thousand, tenthous
  FROM tenk1
  WHERE thousand IN (182, 183) AND tenthous > 7550
--
SELECT thousand, tenthous FROM tenk1 WHERE thousand IN (182, 183) AND tenthous > 7550
== 045
|--
-- Add coverage for optimization of backwards scan index descents
|--
-- Here we expect _bt_search to descend straight to a leaf page containing a
-- non-pivot tuple with the value '47', which comes last (after 11 similar
-- non-pivot tuples).  Query execution should only need to visit a single
-- leaf page here.
|--
-- Test case relies on tenk1_hundred index having a leaf page whose high key
-- is '(48, -inf)'.  We use a low cardinality index to make our test case less
-- sensitive to implementation details that may change in the future.
set enable_seqscan to false
--
SET enable_seqscan TO FALSE
== 046
set enable_indexscan to true
--
SET enable_indexscan TO TRUE
== 047
set enable_bitmapscan to false
--
SET enable_bitmapscan TO FALSE
== 048
explain (costs off)
select hundred, twenty from tenk1 where hundred < 48 order by hundred desc limit 1
--
EXPLAIN (COSTS OFF) SELECT hundred, twenty FROM tenk1 WHERE hundred < 48 ORDER BY hundred DESC LIMIT 1
== 049
select hundred, twenty from tenk1 where hundred < 48 order by hundred desc limit 1
--
SELECT hundred, twenty FROM tenk1 WHERE hundred < 48 ORDER BY hundred DESC LIMIT 1
== 050
-- This variant of the query need only return a single tuple located to the immediate
-- right of the '(48, -inf)' high key.  It also only needs to scan one single
-- leaf page (the right sibling of the page scanned by the last test case):
explain (costs off)
select hundred, twenty from tenk1 where hundred <= 48 order by hundred desc limit 1
--
EXPLAIN (COSTS OFF) SELECT hundred, twenty FROM tenk1 WHERE hundred <= 48 ORDER BY hundred DESC LIMIT 1
== 051
select hundred, twenty from tenk1 where hundred <= 48 order by hundred desc limit 1
--
SELECT hundred, twenty FROM tenk1 WHERE hundred <= 48 ORDER BY hundred DESC LIMIT 1
== 052
|--
-- Add coverage for ScalarArrayOp btree quals with pivot tuple constants
|--
explain (costs off)
select distinct hundred from tenk1 where hundred in (47, 48, 72, 82)
--
EXPLAIN (COSTS OFF) SELECT DISTINCT hundred FROM tenk1 WHERE hundred IN (47, 48, 72, 82)
== 053
select distinct hundred from tenk1 where hundred in (47, 48, 72, 82)
--
SELECT DISTINCT hundred FROM tenk1 WHERE hundred IN (47, 48, 72, 82)
== 054
explain (costs off)
select distinct hundred from tenk1 where hundred in (47, 48, 72, 82) order by hundred desc
--
EXPLAIN (COSTS OFF) SELECT DISTINCT hundred FROM tenk1 WHERE hundred IN (47, 48, 72, 82) ORDER BY hundred DESC
== 055
select distinct hundred from tenk1 where hundred in (47, 48, 72, 82) order by hundred desc
--
SELECT DISTINCT hundred FROM tenk1 WHERE hundred IN (47, 48, 72, 82) ORDER BY hundred DESC
== 056
explain (costs off)
select thousand from tenk1 where thousand in (364, 366,380) and tenthous = 200000
--
EXPLAIN (COSTS OFF) SELECT thousand FROM tenk1 WHERE thousand IN (364, 366, 380) AND tenthous = 200000
== 057
select thousand from tenk1 where thousand in (364, 366,380) and tenthous = 200000
--
SELECT thousand FROM tenk1 WHERE thousand IN (364, 366, 380) AND tenthous = 200000
== 058
|--
-- Check correct optimization of LIKE (special index operator support)
-- for both indexscan and bitmapscan cases
|--

set enable_seqscan to false
--
SET enable_seqscan TO FALSE
== 059
set enable_indexscan to true
--
SET enable_indexscan TO TRUE
== 060
set enable_bitmapscan to false
--
SET enable_bitmapscan TO FALSE
== 061
explain (costs off)
select proname from pg_proc where proname like E'RI\\_FKey%del' order by 1
--
EXPLAIN (COSTS OFF) SELECT proname FROM pg_proc WHERE proname LIKE E'RI\\_FKey%del' ORDER BY 1
== 062
select proname from pg_proc where proname like E'RI\\_FKey%del' order by 1
--
SELECT proname FROM pg_proc WHERE proname LIKE E'RI\\_FKey%del' ORDER BY 1
== 063
explain (costs off)
select proname from pg_proc where proname ilike '00%foo' order by 1
--
EXPLAIN (COSTS OFF) SELECT proname FROM pg_proc WHERE proname ILIKE '00%foo' ORDER BY 1
== 064
select proname from pg_proc where proname ilike '00%foo' order by 1
--
SELECT proname FROM pg_proc WHERE proname ILIKE '00%foo' ORDER BY 1
== 065
explain (costs off)
select proname from pg_proc where proname ilike 'ri%foo' order by 1
--
EXPLAIN (COSTS OFF) SELECT proname FROM pg_proc WHERE proname ILIKE 'ri%foo' ORDER BY 1
== 066
set enable_indexscan to false
--
SET enable_indexscan TO FALSE
== 067
set enable_bitmapscan to true
--
SET enable_bitmapscan TO TRUE
== 068
explain (costs off)
select proname from pg_proc where proname like E'RI\\_FKey%del' order by 1
--
EXPLAIN (COSTS OFF) SELECT proname FROM pg_proc WHERE proname LIKE E'RI\\_FKey%del' ORDER BY 1
== 069
select proname from pg_proc where proname like E'RI\\_FKey%del' order by 1
--
SELECT proname FROM pg_proc WHERE proname LIKE E'RI\\_FKey%del' ORDER BY 1
== 070
explain (costs off)
select proname from pg_proc where proname ilike '00%foo' order by 1
--
EXPLAIN (COSTS OFF) SELECT proname FROM pg_proc WHERE proname ILIKE '00%foo' ORDER BY 1
== 071
select proname from pg_proc where proname ilike '00%foo' order by 1
--
SELECT proname FROM pg_proc WHERE proname ILIKE '00%foo' ORDER BY 1
== 072
explain (costs off)
select proname from pg_proc where proname ilike 'ri%foo' order by 1
--
EXPLAIN (COSTS OFF) SELECT proname FROM pg_proc WHERE proname ILIKE 'ri%foo' ORDER BY 1
== 073
reset enable_seqscan
--
RESET enable_seqscan
== 074
reset enable_indexscan
--
RESET enable_indexscan
== 075
reset enable_bitmapscan
--
RESET enable_bitmapscan
== 076
-- Also check LIKE optimization with binary-compatible cases

create temp table btree_bpchar (f1 text collate "C")
--
CREATE TEMPORARY TABLE btree_bpchar (f1 text COLLATE "C")
== 077
create index on btree_bpchar(f1 bpchar_ops) WITH (deduplicate_items=on)
--
CREATE INDEX ON btree_bpchar USING btree (f1 bpchar_ops) WITH (deduplicate_items=on)
== 078
insert into btree_bpchar values ('foo'), ('fool'), ('bar'), ('quux')
--
INSERT INTO btree_bpchar VALUES ('foo'), ('fool'), ('bar'), ('quux')
== 079
-- doesn't match index:
explain (costs off)
select * from btree_bpchar where f1 like 'foo'
--
EXPLAIN (COSTS OFF) SELECT * FROM btree_bpchar WHERE f1 LIKE 'foo'
== 080
select * from btree_bpchar where f1 like 'foo'
--
SELECT * FROM btree_bpchar WHERE f1 LIKE 'foo'
== 081
explain (costs off)
select * from btree_bpchar where f1 like 'foo%'
--
EXPLAIN (COSTS OFF) SELECT * FROM btree_bpchar WHERE f1 LIKE 'foo%'
== 082
select * from btree_bpchar where f1 like 'foo%'
--
SELECT * FROM btree_bpchar WHERE f1 LIKE 'foo%'
== 083
-- these do match the index:
explain (costs off)
select * from btree_bpchar where f1::bpchar like 'foo'
--
EXPLAIN (COSTS OFF) SELECT * FROM btree_bpchar WHERE f1::bpchar LIKE 'foo'
== 084
select * from btree_bpchar where f1::bpchar like 'foo'
--
SELECT * FROM btree_bpchar WHERE f1::bpchar LIKE 'foo'
== 085
explain (costs off)
select * from btree_bpchar where f1::bpchar like 'foo%'
--
EXPLAIN (COSTS OFF) SELECT * FROM btree_bpchar WHERE f1::bpchar LIKE 'foo%'
== 086
select * from btree_bpchar where f1::bpchar like 'foo%'
--
SELECT * FROM btree_bpchar WHERE f1::bpchar LIKE 'foo%'
== 087
-- get test coverage for "single value" deduplication strategy:
insert into btree_bpchar select 'foo' from generate_series(1,1500)
--
INSERT INTO btree_bpchar SELECT 'foo' FROM generate_series(1, 1500)
== 088
|--
-- Perform unique checking, with and without the use of deduplication
|--
CREATE TABLE dedup_unique_test_table (a int) WITH (autovacuum_enabled=false)
--
CREATE TABLE dedup_unique_test_table (a int) WITH (autovacuum_enabled=false)
== 089
CREATE UNIQUE INDEX dedup_unique ON dedup_unique_test_table (a) WITH (deduplicate_items=on)
--
CREATE UNIQUE INDEX dedup_unique ON dedup_unique_test_table USING btree (a) WITH (deduplicate_items=on)
== 090
CREATE UNIQUE INDEX plain_unique ON dedup_unique_test_table (a) WITH (deduplicate_items=off)
--
CREATE UNIQUE INDEX plain_unique ON dedup_unique_test_table USING btree (a) WITH (deduplicate_items=off)
== 091
-- Generate enough garbage tuples in index to ensure that even the unique index
-- with deduplication enabled has to check multiple leaf pages during unique
-- checking (at least with a BLCKSZ of 8192 or less)
DO $$
BEGIN
    FOR r IN 1..1350 LOOP
        DELETE FROM dedup_unique_test_table;
        INSERT INTO dedup_unique_test_table SELECT 1;
    END LOOP;
END$$
--
DO $$
BEGIN
    FOR r IN 1..1350 LOOP
        DELETE FROM dedup_unique_test_table;
        INSERT INTO dedup_unique_test_table SELECT 1;
    END LOOP;
END$$
== 092
-- Exercise the LP_DEAD-bit-set tuple deletion code with a posting list tuple.
-- The implementation prefers deleting existing items to merging any duplicate
-- tuples into a posting list, so we need an explicit test to make sure we get
-- coverage (note that this test also assumes BLCKSZ is 8192 or less):
DROP INDEX plain_unique
--
DROP INDEX plain_unique
== 093
DELETE FROM dedup_unique_test_table WHERE a = 1
--
DELETE FROM dedup_unique_test_table WHERE a = 1
== 094
INSERT INTO dedup_unique_test_table SELECT i FROM generate_series(0,450) i
--
INSERT INTO dedup_unique_test_table SELECT i FROM generate_series(0, 450) i
== 095
|--
-- Test B-tree fast path (cache rightmost leaf page) optimization.
|--

-- First create a tree that's at least three levels deep (i.e. has one level
-- between the root and leaf levels). The text inserted is long.  It won't be
-- TOAST compressed because we use plain storage in the table.  Only a few
-- index tuples fit on each internal page, allowing us to get a tall tree with
-- few pages.  (A tall tree is required to trigger caching.)
|--
-- The text column must be the leading column in the index, since suffix
-- truncation would otherwise truncate tuples on internal pages, leaving us
-- with a short tree.
create table btree_tall_tbl(id int4, t text)
--
CREATE TABLE btree_tall_tbl (id int4, t text)
== 096
alter table btree_tall_tbl alter COLUMN t set storage plain
--
ALTER TABLE btree_tall_tbl ALTER COLUMN t SET STORAGE plain
== 097
create index btree_tall_idx on btree_tall_tbl (t, id) with (fillfactor = 10)
--
CREATE INDEX btree_tall_idx ON btree_tall_tbl USING btree (t, id) WITH (fillfactor=10)
== 098
insert into btree_tall_tbl select g, repeat('x', 250)
from generate_series(1, 130) g
--
INSERT INTO btree_tall_tbl SELECT g, repeat('x', 250) FROM generate_series(1, 130) g
== 099
insert into btree_tall_tbl select g, NULL
from generate_series(50, 60) g
--
INSERT INTO btree_tall_tbl SELECT g, NULL FROM generate_series(50, 60) g
== 100
|--
-- Test for skip scan with type that lacks skip support (text)
|--
set enable_seqscan to false
--
SET enable_seqscan TO FALSE
== 101
set enable_bitmapscan to false
--
SET enable_bitmapscan TO FALSE
== 102
-- Forwards scan
SELECT id FROM btree_tall_tbl WHERE id = 55 ORDER BY t, id
--
SELECT id FROM btree_tall_tbl WHERE id = 55 ORDER BY t, id
== 103
explain (costs off)
SELECT id FROM btree_tall_tbl WHERE id = 55 ORDER BY t, id
--
EXPLAIN (COSTS OFF) SELECT id FROM btree_tall_tbl WHERE id = 55 ORDER BY t, id
== 104
-- Backwards scan
SELECT id FROM btree_tall_tbl WHERE id = 55 ORDER BY t DESC, id DESC
--
SELECT id FROM btree_tall_tbl WHERE id = 55 ORDER BY t DESC, id DESC
== 105
explain (costs off)
SELECT id FROM btree_tall_tbl WHERE id = 55 ORDER BY t DESC, id DESC
--
EXPLAIN (COSTS OFF) SELECT id FROM btree_tall_tbl WHERE id = 55 ORDER BY t DESC, id DESC
== 106
reset enable_seqscan
--
RESET enable_seqscan
== 107
reset enable_bitmapscan
--
RESET enable_bitmapscan
== 108
|--
-- Test for multilevel page deletion
|--
CREATE TABLE delete_test_table (a bigint, b bigint, c bigint, d bigint)
--
CREATE TABLE delete_test_table (a bigint, b bigint, c bigint, d bigint)
== 109
INSERT INTO delete_test_table SELECT i, 1, 2, 3 FROM generate_series(1,80000) i
--
INSERT INTO delete_test_table SELECT i, 1, 2, 3 FROM generate_series(1, 80000) i
== 110
ALTER TABLE delete_test_table ADD PRIMARY KEY (a,b,c,d)
--
ALTER TABLE delete_test_table ADD PRIMARY KEY (a, b, c, d)
== 111
-- Delete most entries, and vacuum, deleting internal pages and creating "fast
-- root"
DELETE FROM delete_test_table WHERE a < 79990
--
DELETE FROM delete_test_table WHERE a < 79990
== 112
VACUUM delete_test_table
--
VACUUM delete_test_table
== 113
|--
-- Test B-tree insertion with a metapage update (XLOG_BTREE_INSERT_META
-- WAL record type). This happens when a "fast root" page is split.  This
-- also creates coverage for nbtree FSM page recycling.
|--
-- The vacuum above should've turned the leaf page into a fast root. We just
-- need to insert some rows to cause the fast root page to split.
INSERT INTO delete_test_table SELECT i, 1, 2, 3 FROM generate_series(1,1000) i
--
INSERT INTO delete_test_table SELECT i, 1, 2, 3 FROM generate_series(1, 1000) i
== 114
-- Test unsupported btree opclass parameters
create index on btree_tall_tbl (id int4_ops(foo=1))
--
CREATE INDEX ON btree_tall_tbl USING btree (id int4_ops(foo=1))
== 115
-- test parallel build with immutable function.
CREATE TABLE btree_test_expr (n int)
--
CREATE TABLE btree_test_expr (n int)
== 116
CREATE FUNCTION btree_test_func() RETURNS int LANGUAGE sql IMMUTABLE RETURN 0
--
CREATE FUNCTION btree_test_func() RETURNS int LANGUAGE sql IMMUTABLE RETURN 0
== 117
BEGIN
--
BEGIN
== 118
SET LOCAL min_parallel_table_scan_size = 0
--
SET LOCAL min_parallel_table_scan_size TO 0
== 119
SET LOCAL max_parallel_maintenance_workers = 4
--
SET LOCAL max_parallel_maintenance_workers TO 4
== 120
CREATE INDEX btree_test_expr_idx ON btree_test_expr USING btree (btree_test_func())
--
CREATE INDEX btree_test_expr_idx ON btree_test_expr USING btree (btree_test_func())
== 121
COMMIT
--
COMMIT
== 122
DROP TABLE btree_test_expr
--
DROP TABLE btree_test_expr
== 123
DROP FUNCTION btree_test_func()
--
DROP FUNCTION btree_test_func()
== 124
-- Test case of ALTER INDEX with abuse of column names for indexes.
-- This grammar is not officially supported, but the parser allows it.
CREATE INDEX btree_tall_idx2 ON btree_tall_tbl (id)
--
CREATE INDEX btree_tall_idx2 ON btree_tall_tbl USING btree (id)
== 125
ALTER INDEX btree_tall_idx2 ALTER COLUMN id SET (n_distinct=100)
--
ALTER INDEX btree_tall_idx2 ALTER COLUMN id SET (n_distinct=100)
== 126
DROP INDEX btree_tall_idx2
--
DROP INDEX btree_tall_idx2
== 127
-- Partitioned index
CREATE TABLE btree_part (id int4) PARTITION BY RANGE (id)
--
CREATE TABLE btree_part (id int4) PARTITION BY RANGE (id)
== 128
CREATE INDEX btree_part_idx ON btree_part(id)
--
CREATE INDEX btree_part_idx ON btree_part USING btree (id)
== 129
ALTER INDEX btree_part_idx ALTER COLUMN id SET (n_distinct=100)
--
ALTER INDEX btree_part_idx ALTER COLUMN id SET (n_distinct=100)
== 130
DROP TABLE btree_part
--
DROP TABLE btree_part
